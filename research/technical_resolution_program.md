# B1-B10 Technical Resolution Program v0.1

Last updated: 2026-06-22

Purpose: keep the portfolio focused on solving the ten technical problems first.
Publication, patent, financing, and product outcomes are downstream consequences
of technical resolution; they are not the current success criterion.

## Current Priority Order

1. **B1 -> B2 -> B7 systems spine.** This is the strongest path because B1
   already has replayable proof evidence, B2 has a real Stim/PyMatching baseline,
   and B7 connects them into resource consequences.
2. **B4 -> B8 verification spine.** This is the second path because it has a
   clear failure boundary: adaptive spoofers become dangerous at high leakage.
3. **B10/B9 theory guardrails.** These prevent us from overclaiming and can
   produce formal negative results.
4. **B3/B5/B6 application spine.** These require stronger classical baselines
   and real data before they can become solution claims.

## What Counts As Solving Each Problem

| ID | Technical solution target | Current evidence | Main blocker | Next technical experiment |
|---|---|---|---|---|
| B1 | A compiler method that reduces routed two-qubit work and exposure across external benchmarks while emitting replayable semantic certificates. | 30/30 circuits; 481 virtual SWAPs removed; 37.18% 2Q reduction; 481/481 proof replay; post-virtual-SWAP 1Q resynthesis gives 1.018x logical T-count proxy; native Z-phase canonicalization gives a further 1.000990x; targeted control-RZ commuting removes 380 RZ gates and gives 1.119x logical T-count proxy over the native pass; U3 phase factoring now factors 1641 U3 gates, removes 313 additional RZ gates after factoring, emits 1641 factorization certificate events plus 3242 RZ-commute certificate events, passes 30/30 Aer cross-checks, and gives a further 1.036371x logical T-count proxy over control-RZ. T-B1-004a exposes 270 arbitrary decimal rotations and 3 target cone classes; T-B1-004b shows `cone_01` has 35 pair-local single-arbitrary windows; T-B1-004c gets 0 exact-pass windows for same-envelope phase removal; T-B1-004d lets neighboring RZ phases reabsorb while locking RY to 9 exact candidates and still gets 0 exact-pass windows, best/median residual 0.21253656711362606 / 0.3643516233170531; T-B1-004e finds all 35 `cone_01` windows have nonzero RY parameter sensitivity, 0 are near the pi/4 exact grid, and the windows use 4 distinct theta groups; T-B1-004f records 31 duplicate theta occurrences and an optimistic 620 proxy-T cache-reuse signal, but the occurrence ledger still counts 0 removed occurrences and 0 proxy-T reduction; T-B1-004g checked the physical theta-sharing cost-model route at 0/8 gates; T-B1-004h defines 4 shared-theta synthesis object proposals covering all 35 windows; T-B1-004i replay-verifies 4/4 objects and 35/35 occurrences against source QASM and parameter-transfer groups with 0 mismatches; T-B1-004j adds logical anchor/route packets for all 4 objects and all 35 occurrences with total/max logical hops 139/11; T-B1-004k adds a factory-amortization scaffold where 35 baseline synthesis requests collapse to 4 shared-object requests, giving 31 amortized saved compiles and gross proxy-T pressure delta 620; T-B1-004l adds a shared-error budget scaffold with 1e-6 aggregate synthesis-error budget, 4 correlation groups, and max correlated occurrence count 16; T-B1-004m adds an independent accounting baseline with 0 double-counted occurrences and 0 double-counted proxy-T pressure; T-B1-004n attempts CM-08 explicitly and rejects the current model in the refreshed B7 ledger; T-B1-004o adds a local-equivalence invariant obligation gate where 24/35 windows have nonzero invariant sensitivity to `RY(theta)`, 24 mismatch the nearest pi/4-grid invariant, and 11 remain invariant-flat; T-B1-004p groups those 11 invariant-flat windows into 3 normalized residual pattern groups, all sharing partner q[14], and shows that solving all 11 would remove at most 11 occurrences / 220 proxy-T, leaving a 19-occurrence / 380 proxy-T B7 gap; T-B1-004q turns those 3 groups into KAK/nonlocal-invariant packets: all 3 share one numerical nonlocal fingerprint and match nearest pi/4-grid nonlocal fingerprints, but same-envelope grid replacement has 0 exact passes and leaves 3 local-dressing/rewrite obligations. T-B1-004r then numerically solves those 3 local-dressing obligations with arbitrary SU(2)xSU(2) dressing at max residual 4.710277376051325e-16, but the dressings contain off-pi/4-grid local Euler parameters, accepted occurrence removal is 0, and accepted proxy-T reduction is 0. T-B1-004s projects those dressings to the pi/4 grid and gets 0/3 exact passes, projected residuals 0.3000-0.8416, 3 distinct grid signatures, 26 off-grid local dressing parameters, and 0 single-parameter snap exact passes, closing the direct rounding/shared-signature route. T-B1-004t then enumerates 24 one-qubit Clifford representatives, 576 pair-local Clifford representatives, and 331,776 left/right pair-local Clifford dressings per packet; it finds 0/3 exact packets and best residual range 0.2125-0.3644, closing the plain local Clifford dressing route. T-B1-004u then checks 143,327,232 single-carrier local dressing candidates and exactifies all 3 flat packets with residuals 3.20e-16-4.68e-16, but one arbitrary local carrier remains per packet and accepted occurrence/proxy-T reduction stays 0. T-B1-004v consumes those exact packets in the occurrence ledger: 3 unique carrier signatures cover 11 flat occurrences, 11 carrier occurrences are inserted for 11 originals, net arbitrary-occurrence delta is 0, optimistic template reuse is 160 proxy-T but unaccepted, and even full carrier absorption would still miss the B7 target by 19 occurrences / 380 proxy-T. T-B1-004w rejects natural carrier coalescence with 3 distinct signatures, 0 cross-pattern shareable signatures, largest signature 8 occurrences, and only 160 optimistic proxy-T reuse. T-B1-004x checks native rotation inventory: 2049 rotation arguments, 2/3 carrier patterns have inventory/same-target angle matches, flat_pattern_02 has no inventory-angle match, 0/3 patterns have line-local absorption candidates, and accepted reduction remains 0. T-B1-004y checks carrier neighborhood/commutation candidates: 2/3 patterns have same-target inventory matches, but 0/3 have radius-4 candidates, only flat_pattern_01 has a radius-16/blocker-free candidate, flat_pattern_02 has no same-target match, flat_pattern_03 nearest same-target match is 99 lines away, and accepted reduction remains 0. The updated cost-model gate is 6/8 passed and 2/8 failed, with accepted B7 ledger reduction still 0. | No calibrated/live-like heavy-hex baseline; dynamic-circuit/reset/classical-control gaps; FT synthesis ledger re-costs `sat_n11` as exact Clifford+T rotations and moves it to data-path dominance under parallel factories, but the new B7 min-STV boundary is `gcm_h6` at 1.086008x; target and cone-feasibility gates now isolate `cone_01`, same-envelope phase/Euler absorption routes are closed, theta cache sharing is not counted as an FT resource delta, the proposed physical cost model now has shared object proposals, line-level replay verification, logical layout/routing packets, factory-amortization scaffold, shared-error budget scaffold, independent accounting baseline, and an explicit refreshed-ledger rejection, and the local-equivalence invariant/residual/KAK-packet/local-dressing/dressing-absorption gates show that 24 windows block local-only absorption while the remaining 11-window flat subset is nonlocal-class tractable and numerically local-dressable, but direct pi/4 projection/shared-signature exactification and plain local Clifford dressing fail, while T-B1-004u exactifies the 3 flat packets only by leaving one arbitrary carrier per packet, T-B1-004v confirms the current ledger sees that as replacement rather than removal, T-B1-004w rejects natural carrier coalescence, and T-B1-004x shows inventory-angle matches are partial and never line-local; the carrier remains resource-unaccepted and the subset is insufficient by itself. It still lacks 30 occurrence-removing certificates, a stronger carrier absorption theorem or replayable exactification certificate, accepted physical device layout, physical factory schedule, and device-calibrated physical validation. There is still no rewrite, no semantic certificate, no obstruction theorem, and no resource reduction. | Run `T-B1-004`: synthesize or prove a broader local two-qubit semantic rewrite, KAK/Clifford scaffold, or alternate certificate-bearing transformation for at least 30 `cone_01` windows while explicitly carrying, sharing, or eliminating theta with replayable certificates; use the 3 invariant-flat residual pattern groups as bounded local-dressing/rewrite subproblems only if the method goes beyond direct pi/4 projection, shared grid signatures, plain local Clifford dressing, and the current single-carrier packet without occurrence removal; prove a stronger carrier-absorption theorem or replayable broader two-qubit rewrite certificate that turns ledger replacement into accepted occurrence removal while defeating the T-B1-004x inventory-only boundary and T-B1-004y neighborhood-only boundary, and also supply at least 19 additional occurrence certificates or replace the rejected CM-08 route with genuinely accepted physical layout/factory/device-calibrated evidence and a B7 ledger improvement. |
| B2 | A code/schedule/decoder comparison that beats surface-code target volume under the same confidence-bounded metric. | Stim/PyMatching baseline plus closed reduced-round artifact boundary; T-B2-003 leakage-flagged analytic boundary; T-B2-004 Stim HERALDED_ERASE / DEPOLARIZE1 stress; T-B2-005 false-positive stress; T-B2-006 posterior-calibrated shot-conditioned leakage boundary; T-B2-007 posterior-weighted decoder-risk ledger; T-B2-008 decoder-input contract feasibility gate; T-B2-009a per-shot decoder trace packet; T-B2-009b posterior-likelihood injection gate; T-B2-009c DEM-informed detector-to-edge semantics gate; T-B2-009d hardware-like leakage observation model gate; T-B2-009e calibration-transfer guardrail; and T-B2-010 calibrated-evidence contract gate. The latest contract consumes the guardrail, checks 8 requirements, passes 5, fails K4/K5/K6, and emits 3 PR packets for calibrated flag data, hardware trace replay, and holdout improvement. Best conservative holdout failures remain 16 baseline / 16 injected / delta 0, so calibration transfer, production decoder readiness, and threshold support remain false. | The reduced-round lever remains closed. The heralded-erasure route now has per-shot traces, synthetic posterior-weight injection, DEM-informed edge semantics, a detector-bitstring-driven hardware-like leakage observation model, an explicit calibration-transfer guardrail, and PR-ready evidence contracts, but none improves failures. The model is still not calibrated, not hardware-derived, and not a production decoder. The route still lacks real calibrated leakage/flag observations, independent hardware traces, strict holdout improvement, and distance-scaling runtime/threshold curves under calibrated posterior injection. | Satisfy T-B2-010 K4-K6: submit calibrated leakage/flag observations, real or independently calibrated hardware traces replayed through the same decoder interface, and strict holdout improvement with all-challenge non-regression before feeding B7. |
| B3 | A reaction-coordinate quantum resource estimate tied to observable error and classical baselines. | PySCF proxy through T-B3-011 pressure plus T-B3-012 same-access measurement-rescue gate and T-B3-013 same-access negative boundary note. The latest note records 9/9 satisfied negative-boundary conditions over the failed rescue gate: source rescue remains 5/10 passed, failed source gates are M5-M9, denominator wins remain 0, max optimizer-loop shots lower bound is 475,043,013,690,000, max optimizer-loop 2Q lower bound is 281,225,464,104,480,000, and B10 still rejects the current B3 sampling bridge. No reaction-dynamics solution, quantum advantage, BQP separation, or positive same-access route is claimed. | B3 is formally demoted until full compiled-state covariance, multi-parameter/converged chemistry state prep, same-access denominator wins, optimizer-loop reduction, and B10 access-contract acceptance replace M5-M9. | Do not spend more B3 effort unless a PR replaces M5-M9; otherwise move effort to B5/B10 production DMRG/MPS or a fully costed response oracle. |
| B4 | A circuit-level verifiable advantage task with explicit completeness/soundness against adaptive spoofers. | Toy hidden-trap protocol rejects four spoofing families; shared B4/B8 CNOT hidden-projection refresh task now runs 3 circuit-derived tasks / 192 configs with honest completeness 1.0, no-refresh high-leakage max soundness 0.675, and repaired high-leakage max soundness 0.0. T-B4-002a exports an OpenQASM 3 randomized-measurement packet: 36 hardware-executable verifier circuits, max 30 qubits with ancillas, all headers `OPENQASM 3.0`, and Qiskit/Aer semantic mismatch count 0. T-B8-003a adds a public-QASM packet spoofer gate: a deterministic parser/emulator predicts all 36 public packet transcripts, so public-packet protocol soundness is rejected. T-B8-003b separates public skeletons from private masks/flips for all 36 circuits, but public data transcripts remain deterministic and classically predictable; the contract passes 4/8 gates and fails 4/8. T-B8-003c adds non-stabilizer H plus T/RZ(pi/4) challenge-basis layers to all 36 skeletons, removes the deterministic transcript blocker for 36/36 pilot circuits, and records minimum min-entropy 4 bits with maximum output probability 0.0625. T-B8-003d attacks the pilot with four support-aware spoofer families: exact transcript success remains 0.0625, but support-only verifier acceptance is 1.0, so public support membership is rejected as a soundness condition. T-B8-003e adds four verifier-private predicate bits: hidden-private-predicate acceptance is 0.0625 instead of public support-only acceptance 1.0, while one-bit leakage gives 0.125 and full predicate leakage returns 1.0. | The exact transcript blocker survives and analytic verifier-private predicates suppress support spoofers, but this is still not a formal protocol or hardware result. Soundness still requires a formal private challenge protocol, real backend properties, hardware execution, and stronger learned/generative spoofers under leakage. | Replace the analytic private-predicate pressure gate with a formal verifier-private challenge protocol or real-backend/hardware transcript burden, then rerun learned/generative spoofer gates with explicit leakage models. |
| B5 | A hybrid quantum-tensor/embedding solver that improves accuracy-per-resource on a meaningful correlated model. | Exact small Hubbard + cluster proxy; cluster-4 improves over cluster-2. B5 now has oracle-tuned and non-oracle response embedding denominators, exact-state-seeded MPS pressure, non-exact-state-seeded MPS/ALS, two-site finite-DMRG-style pressure, T-B5-005 canonical DMRG readiness gate, T-B5-006a canonical-environment smoke gate, and T-B5-006b same-access production contract gate on the same 9 B10 D5 Hubbard response rows. T-B5-006d/T-B10-014b executes W4 as a row-contract harness. T-B5-006e/T-B10-014c executes W2 as a seeded-pressure replacement audit: 3 candidate families replayed, 0 deployable replacements, and max row-level win count 2/9. T-B5-006f/T-B10-014d executes W3 as a same-access response-oracle cost ledger: 8 oracle requirements checked, 3 passed, 5 failed, no response oracle constructed, and 0 rows beating explicit D5 matvec pressure for the seeded target. T-B5-006g/T-B10-014e executes the W1 acceptance gate: 10 production-denominator requirements checked, 3 passed, 7 failed, failed IDs D3-D9, and no production denominator available. T-B5-006h/T-B10-014f executes W1 denominator-engine v0: 8 requirements checked, 4 passed, 4 failed (E4-E7), 216 sweep rows preserved, 0 convergence-passed rows, and 0 rows beating seeded pressure. T-B5-006i/T-B10-014g executes the W1 canonical residual blocker gate: 8 requirements checked, 4 passed, 4 failed (C3/C4/C5/C7), 0 environment rows, 0 residual rows, 0 convergence-passed rows, and 4 PR packets emitted. | The current tensor portfolio is a pressure ladder, not mature canonical-environment DMRG. W2 did not replace seeded pressure, W3 did not instantiate a same-access response oracle, the W1 acceptance gate failed D3-D9, W1 denominator-engine v0 failed E4-E7, and the canonical residual blocker gate failed C3/C4/C5/C7. The row-contract, W2, W3, W1 acceptance, W1 denominator, and canonical-residual audits prevent row drift and unsupported replacement/oracle/production claims, but they do not yet build a production solver. | Replace W1 denominator-engine v0 with production DMRG/MPS preserving the T-B5-006d row-contract hash, satisfying T-B5-006g/T-B5-006h/T-B5-006i, and beating seeded pressure plus the full denominator ladder; only reopen W2/W3 if new evidence supplies a stronger denominator or a real response oracle. |
| B6 | A leakage-controlled high-Tc candidate ranking pipeline using physics descriptors, not family priors. | Toy descriptor ranker plus T-B6-001 curated leakage audit, T-B6-002 formula-derived descriptor screen, T-B6-003 structural/electronic proxy boundary, T-B6-004 crystallographic reproducibility gate, and T-B6-005a crystallographic evidence contract. The latest contract checks 8 requirements, passes 3, fails K4-K8, preserves R6-R10, and emits 5 PR packets: reproducible crystallographic backend, source-validation cleanup, family-prior denominator defeat, DFT observable channel, and B5-computed observable channel. No discovery, mechanism, complete-database, DFT-observable, B5-observable, reproducible crystallographic descriptor, or solution claim. | B6 has reached a contract-ready handoff, but promotion is blocked until the five packets pass. | Close one or more T-B6-005 packets while preserving the 56-record / 28-family / 18-negative-control scope and rerun the audit before any candidate-ranking claim. |
| B7 | An end-to-end fault-tolerant resource ledger where B1/B2 improvements survive workload DAGs, factories, layout, and feed-forward. | B1/B2 dependency bridge: 1.195x-1.616x conservative STV reduction; workload-DAG factory schedule: mean 1.475x; original logical T-factory schedule exposed a 1.0x min-STV boundary; control-RZ commute schedule improves min STV to 1.121951x; U3 phase-factored schedule preserves min STV at 1.121951x and improves mean STV to 1.234394x; FT synthesis ledger exposes `gcm_h6` as the new min row at 1.086008x; T-B7-004 through T-B7-010a show precision-aware budgets, local same-axis merge, shared cache, nonlocal adjacent cancellation, four tested `w8_21` rewrite families, and one-angle retained-template routes do not produce a certified occurrence-removing saving. T-B1-004c/d show same-envelope `cone_01` phase-removal and Euler-reabsorption routes have 0 exact-pass windows; T-B1-004e shows all 35 candidate RY parameters are sensitive and none are pi/4-grid exact; T-B1-004f shows 31 duplicate theta occurrences and 620 optimistic cache-reuse proxy-T units, but the accepted occurrence ledger still counts 0 removed occurrences and 0 proxy-T reduction; T-B1-004g rejected the physical theta-sharing cost-model route at 0/8 gates; T-B1-004h adds 4 shared-theta object proposals covering all 35 windows; T-B1-004i replay-verifies 4/4 objects and 35/35 occurrences with 0 mismatches; T-B1-004j assigns logical anchor/route packets for all 4 objects and 35 occurrences with total/max logical hops 139/11; T-B1-004k adds a factory-amortization scaffold where 35 baseline synthesis requests collapse to 4 shared-object requests, giving 31 amortized saved compiles and gross proxy-T pressure delta 620; T-B1-004l adds a 1e-6 shared-error budget across 4 correlation groups; T-B1-004m adds an independent accounting baseline with 0 double-counted occurrences and 0 double-counted proxy-T pressure; T-B1-004n attempts CM-08 but rejects the current model in the refreshed B7 ledger; T-B1-004o blocks local-only absorption for 24/35 windows with a local-equivalence invariant diagnostic; T-B1-004p isolates the remaining 11 invariant-flat windows into 3 work packets while proving this subset alone still misses the 30-occurrence target by 19 occurrences / 380 proxy-T; T-B1-004q shows those 3 packets share one nonlocal fingerprint and all match nearest pi/4-grid nonlocal fingerprints, but same-envelope grid replacement has 0 exact passes; T-B1-004r gives exact numerical local dressing for all 3 packets while preserving 0 accepted occurrence removal and 0 accepted proxy-T reduction because the dressing parameters remain off-grid and unaccounted; T-B1-004s projects those dressings to the pi/4 grid, gets 0/3 exact passes with projected residuals 0.3000-0.8416, finds 3 distinct grid signatures and 26 off-grid local dressing parameters, and still accepts 0 occurrence/proxy-T reduction; T-B1-004t enumerates 24 one-qubit Clifford representatives, 576 pair-local Clifford representatives, and 331,776 left/right pair-local Clifford dressings per packet, finding 0/3 exact packets; and T-B1-004u checks 143,327,232 single-carrier local dressing candidates and exactifies all 3 flat packets while leaving one arbitrary carrier per packet, preserving 0 accepted reduction. T-B1-004v then applies the occurrence ledger and shows 11 inserted carrier occurrences for 11 original flat occurrences, net arbitrary-occurrence delta 0, accepted reduction 0, and a 19-occurrence / 380 proxy-T gap even if all carriers were absorbed. T-B1-004w then rejects natural carrier coalescence with 3 distinct signatures, 0 cross-pattern shareable signatures, largest signature 8 occurrences, and only 160 optimistic proxy-T reuse. T-B1-004x then checks carrier absorption inventory: 2049 native rotation arguments, 2/3 carrier patterns have inventory/same-target matches, flat_pattern_02 has no inventory-angle match, 0/3 patterns have line-local absorption candidates, and accepted reduction remains 0. CM-02 through CM-07 pass, CM-08 fails, and the updated cost-model gate is 6/8 passed, 2/8 failed, with B7 ledger reduction still 0. | `gcm_h6` remains factory-path dominated; `cone_01` is still the concrete B1 rewrite target, but simple same-envelope routes are closed, no-carrier deletion is blocked, cache-only theta sharing is not accepted, the physical cost-model proposal now has shared object proposals, line-level replay verification, logical layout/routing packets, factory-amortization scaffold, shared-error budget scaffold, independent accounting baseline, and an explicit refreshed-ledger rejection, and the local-equivalence invariant/residual/KAK-packet/local-dressing/dressing-absorption/local-Clifford gates block 24 local-only absorption routes while making the remaining 11-window flat subset nonlocal-class tractable and numerically local-dressable, but direct pi/4 exactification/shared grid signatures and plain local Clifford dressing fail, while the T-B1-004u/v/w/x single-carrier exact packet remains too small, carrier-bearing, ledger-replacement-only, inventory-only, and uncertified for resources. B7 still lacks occurrence-removing certificates, a stronger carrier absorption theorem or replayable exactification certificate, accepted physical device layout, physical factory schedule, device-calibrated physical validation, and any physical resource claim. | Run `T-B1-004`: produce a certified broader `cone_01` occurrence-removing rewrite for at least 30 windows with explicit theta accounting, solve the 3 invariant-flat residual pattern groups only as part of a larger certificate set by turning the single-carrier exact packets into occurrence-removing certificates that defeat the T-B1-004v replacement ledger and T-B1-004x inventory-only boundary and T-B1-004y neighborhood-only boundary, or using a method beyond direct pi/4 projection/shared-signature/local-Clifford exactification, or reverse the rejected CM-08 with accepted physical model evidence; then re-run B7 only if a rewrite certificate or accepted cost-model certificate exists. |
| B8 | A classical verifier whose soundness stays low under adaptive leakage through challenge refresh/projection rotation. | Honest completeness 1.0; five adversaries rejected; 0.75 leakage failure boundary; toy refresh/rotation repair makes high-leakage repair modes pass <=5% soundness; shared B4/B8 circuit-refresh task instantiates refresh/rotation on CNOT hidden-projection tasks and reduces high-leakage adaptive soundness from 0.675 without refresh to 0.0 under the best repaired modes; T-B4-002a exports 36 OpenQASM 3 verifier circuits with 0 Aer semantic mismatches; T-B8-003a public-QASM packet spoofer gate predicts all 36 public packet transcripts and rejects public-packet protocol soundness; T-B8-003b late-bound contract creates 36 public skeletons that hide private masks/flips, but the deterministic public data transcript remains classically predictable; 4/8 gates pass and 4/8 fail; T-B8-003c non-stabilizer late-bound transcript pilot adds H plus T/RZ(pi/4) challenge-basis layers, removes the deterministic transcript blocker for 36/36 pilot circuits, and records minimum min-entropy 4 bits with maximum output probability 0.0625; T-B8-003d support-aware spoofer gate covers four public-support spoofer families across 36 pilot circuits: max exact transcript success stays 0.0625, but max support-only acceptance is 1.0, rejecting support-membership soundness; T-B8-003e verifier-private predicate pressure gate lowers hidden-private-predicate acceptance to 0.0625 versus public support-only acceptance 1.0, with leakage degradation to 0.125 for one private bit leaked and 1.0 for full predicate leakage; trained/generative spoofer stress runs 144 configs and shows no-refresh high leakage reaches learned soundness 1.0; B10-T2 proof-obligation gate rejects no-refresh high-leakage claims; restricted lemma gives a 8.94e-44 single-unknown-mask bound; transcript leakage simulator runs 192 configs; device-noise transcript bridge runs 480 configs and keeps challenge_refresh/refresh_plus_rotation high-leakage soundness <=0.0208 under bounded noise; Qiskit/Aer verifier bridge instantiates 216 ideal circuits; noisy Aer bridge executes 9600 circuits with safe adversary acceptance 0.0; backend-calibrated-style bridge derives per-qubit/per-edge Aer noise from Qiskit GenericBackendV2 target InstructionProperties and executes 5760 circuits with safe calibrated honest acceptance 1.0, adversary acceptance 0.25, and max honest predicate-bit error 0.0703125. | The public/private boundary and non-stabilizer entropy remove single-transcript prediction, and private predicates suppress support-only spoofing in an analytic no-leakage model. Soundness still requires a formal private challenge protocol, real backend properties, hardware execution, or another non-public acceptance burden that survives leakage. | Replace the analytic private-predicate model with a formal verifier-private challenge protocol or real-backend/hardware verifier bridge, then attack the resulting transcript with stronger learned/generative spoofers and explicit leakage models. |
| B9 | A formal restricted theorem or negative lemma about gap amplification/local-Hamiltonian hardness. | 18-instance gap lab; 9 locality-preserving candidates; 0 accepted local candidates; finite-instance failed gap-amplification negative lemma with 4 strict width-trap counterexamples, 9 dense locality traps, and 5 proof obligations; Lean-style symbolic skeleton with 5 definitions, 3 theorem skeletons, and the same 5 open obligations; named-family `cluster_stabilizer_open_uniform_reweight` skeleton for n=4,5,6 showing all terms uniformly scaled by 1.35, max locality preserved at 3, raw gap amplified, normalized gap invariant, and the certificate rejected as global energy rescaling; local parametric certificate checker for n >= 4 formulas with exact scale 27/20, support set {2,3}, max locality 3, finite rows n=4,5,6, and normalized-gap invariance checked by a repo-local verifier; proof-environment readiness gate with 4/9 gates passed; and proof-environment contract gate with 3/8 requirements passed, K4-K8 failed, and five PR-ready packets for Lean toolchain, Lake tooling, Lake/mathlib project files, indexed theorem replacement, and checked formal output. | The named-family statement now has a local executable verifier and an assignable proof-environment contract, but it is still not proof-assistant checked over all n; the current PATH `lean` command exits with failure, `lake` is absent, no Lake/mathlib project files exist, and the named-family theorem remains a placeholder `True` obligation. It is not a Quantum PCP proof, NLTS theorem, local-Hamiltonian hardness theorem, or global gap-amplification impossibility result. | Run `T-B9-004`: close the five T-B9-004c packets, pin a real Lean 4/Lake/mathlib project or equivalent independently proof-checkable environment, make the cluster-stabilizer skeleton check inside it, replace the placeholder `True` theorem with an indexed Hamiltonian-family theorem, and formalize support-size, uniform-scaling, spectral-width, and normalized-gap invariance lemmas for all n >= 4. |
| B10 | Formal theorem targets that separate robust BQP advantage from fragile input/oracle/verification assumptions. | 12-node/14-edge reduction graph, formal target cards, explicit-I/O and denominator notes, B3/B5 denominator boundary comparison, B5 same-access bridge, response-sampler cost stress, B5 production contract, and B3 T-B3-013 same-access negative boundary note. B3 is closed under 9/9 negative-boundary conditions unless M5-M9 are replaced. T-B5-006d/T-B10-014b adds a row-contract harness to B10-T1, T-B5-006e/T-B10-014c adds a failed seeded-pressure replacement audit, T-B5-006f/T-B10-014d adds a failed W3 response-oracle ledger with 3/8 oracle requirements passing and no constructed response oracle, T-B5-006g/T-B10-014e adds a W1 acceptance gate with 3/10 requirements passing and D3-D9 failing, T-B5-006h/T-B10-014f adds W1 denominator-engine v0 with 4/8 requirements passing and E4-E7 failing, and T-B5-006i/T-B10-014g adds canonical-residual blocker packets for E4-E7. B10-T2 remains a verifier proxy stack through backend-calibrated-style GenericBackendV2. | B10-T1 now has B3 same-access negative boundary, B5 production-contract failure, row-contract preservation, failed W2 replacement audit, failed W3 response-oracle ledger, failed W1 acceptance, failed W1 denominator-engine v0, and a canonical residual blocker gate with 4 PR packets. These guardrails prevent unsupported denominator/oracle claims, but they do not support quantum advantage, BQP separation, or a positive same-access route. | Run `T-B10-014` through a real B5 W1 production DMRG/MPS denominator engine while preserving the row-contract hash and satisfying T-B5-006g/T-B5-006h/T-B5-006i; reopen B3 only if M5-M9 are replaced by same-access evidence, and reopen W3 only if a real same-access response oracle is instantiated. |

## 30-Day Technical Gates

| Week | Gate | Required evidence |
|---:|---|---|
| 1 | B1 live-like baseline and B8 refresh design | New B1 diagnostic or explicit blocker; B8 refresh spec. |
| 2 | B2 candidate comparison and B7 real DAG draft | B2 expanded result; B7 workload DAG schema. |
| 3 | B3/B5/B6 application baseline lift | B3 negative-boundary rescue gate; B5 readiness gate failed 0/8 production conditions, B5 canonical-environment smoke gate passed 0/9 rows, and the B5/B10 production contract passes only 2/10 gates; B6 crystallographic evidence contract completed and leaves 5 PR packets open; next B5/B6 gates are mature canonical DMRG/MPS or a pinned B6 crystallographic/DFT/B5-observable pipeline that beats family-prior AP. |
| 4 | B9/B10 formal guardrails and keep/merge/kill review | B5 readiness, smoke-gate, production-contract, and production-triage failures converted into T-B5-006/T-B10-014 work packets; B3 demotion note; or B10-T2 real-backend verifier; updated execution board. |

## Downstream Translation Rule

No direction moves into the publication/patent/financing/tool pipeline as an
active deliverable until it passes its technical gate. Translation artifacts may
exist as parking-lot notes, but they do not define current success.

 Future work must beat the T-B1-004x inventory-only boundary, the T-B1-004y neighborhood-only boundary, the T-B1-004z source-alignment-only boundary, the T-B1-004aa blocker-stack boundary, the T-B1-004ab blocker-motif boundary, the T-B1-004ac CNOT-parity boundary, and the T-B1-004ad interleaving-commutation boundary with line-local, blocker-free, source-aligned, reusable-motif-defeating, parity-clearance-defeating, interleaving-commutation-defeating CNOT-stack rewrite, commutation-certified, semantic-replay, or broader occurrence-removing evidence.


B1/B7 update: T-B1-004ad adds a carrier interleaving-commutation gate. Across 3 blocked source-aligned candidates it finds 18 interleaving single-qubit ops on 13 unique lines: 7 cheap control-side phase commutations, 4 target-side phase obstructions, and 7 non-diagonal obstructions. Accepted commutation clearance, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0. Future work must beat the T-B1-004ad boundary with real semantic CNOT-stack synthesis/replay, broader occurrence-removing certificates, or accepted physical-model evidence.

B1/B7 update: T-B1-004ae constructs exact semantic replay packets for the 3 blocked cone_01 carrier CNOT stacks. All 3 packets are 2-qubit 4x4 matrix targets with stable fingerprints, covering 32 window gates total (14 CNOT and 18 single-qubit gates). This is not a solution: semantic replay certificates, shorter rewrites, accepted occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0. Next work must consume these packets in a real synthesis/replay gate.

B1/B7 update: T-B1-004af searches fixed-direction reduced-CNOT scaffolds over the three exact T-B1-004ae packet targets. It finds numerical exact reduced-CNOT candidates for all 3 packets: line 1378 reaches 1 CNOT from source 4, while lines 1381 and 268 reach 2 CNOTs from source 5. Candidate CNOT reduction would be 9 if later accepted, but replay certificates, local-U3 resource accounting, accepted occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ag consumes the T-B1-004ae semantic packets and T-B1-004af reduced-CNOT candidates. The 3 bounded packet targets remain numerically replay-consistent, but replacement off-pi/4 local-U3 parameters rise from 1 source parameter to 40 replacement parameters, adding 780 proxy-T pressure. The route is rejected as a B7 ledger improvement: accepted full-circuit replay certificates, occurrence removal, proxy-T reduction, and B7 improvement remain 0.

B1/B7 update: T-B1-004ah consumes the T-B1-004af/T-B1-004ag reduced-CNOT packet candidates and tests direct pi/4-grid exactification of their local-U3 layers. All 40 replacement off-grid parameters can be projected to the grid, but replay breaks: exact snapped packet passes are 0/3 and the snapped residual range is 0.4757435265-0.7803612881. Accepted local-U3 exactification, absorption certificates, full-circuit replay, occurrence removal, proxy-T reduction, and B7 improvement remain 0. Future work must use stronger symbolic/local synthesis, context absorption with replay certificates, or a different occurrence-removing scaffold.

B1/B7 update: T-B1-004ai consumes T-B1-004ah and searches 420 sparse local-U3 repairs where the pi/4-snapped scaffold frees only one or two local parameters. Line 1378 has an exact one-parameter grid-choice repair, but line 1381 and line 268 remain unrepaired even with two free parameters. This is partial bounded-packet evidence only: accepted full-circuit rewrite, occurrence removal, proxy-T reduction, and B7 improvement remain 0.

B1/B7 update: T-B1-004aj consumes T-B1-004ai and exhaustively searches exactly-three-parameter local-U3 repairs for the two unresolved packets. It checks 1,632 candidates, exactifies line 268 with residual 6.398929014192638e-13, and leaves line 1381 unresolved with best residual 0.049865177666770955. Total bounded packet repairs are now 2/3, partial candidate CNOT reduction would be 6 if later accepted, but accepted full-circuit rewrite, occurrence removal, proxy-T reduction, and B7 improvement remain 0.

B1/B7 update: T-B1-004ak consumes T-B1-004aj and exhaustively searches exactly-four-parameter local-U3 repairs for the one remaining unresolved packet, line 1381. It checks 3,060 candidates and improves the best residual from 0.049865177666770955 to 0.02997767950993884, but finds 0 exact repairs. Total bounded packet repairs remain 2/3, partial candidate CNOT reduction would still be 6 if later accepted, and accepted full-circuit rewrite, occurrence removal, proxy-T reduction, and B7 improvement remain 0.

B1/B7 update: T-B1-004al consumes T-B1-004ak and searches exactly-five-parameter local-U3 repairs for line 1381 on the same two-CNOT pi/4-snapped scaffold. It finds a first exact packet repair after 5,795 of 8,568 deterministic combinations, with residual 6.513934436930801e-13. The bounded packet-level repair set is now 3/3 and candidate CNOT reduction would be 9 if later accepted, but accepted full-circuit rewrite, symbolic exact decomposition, occurrence removal, proxy-T reduction, and B7 improvement remain 0 because the repair still carries five off-grid local-U3 parameters and no full-circuit replay certificate.

B1/B7 update: T-B1-004am consumes the line-1378 sparse repair, the line-268 three-parameter repair, and the line-1381 five-parameter repair as one repaired-packet resource-boundary gate. The route keeps 3/3 bounded packet repairs and the 9-CNOT candidate reduction signal, while reducing replacement off-grid local-U3 parameters from 40 to 5 and incremental proxy-T pressure from 780 to 80. This is a real narrowing of the blocker, but accepted full-circuit replay certificates, occurrence removal, proxy-T reduction, and B7 improvement remain 0. Future work must exact-decompose or absorb the five line-1381 off-grid local-U3 parameters and emit symbolic full-circuit replay certificates before any B7 ledger saving can be counted.

B1/B7 update: T-B1-004an consumes T-B1-004am and tests those five remaining line-1381 off-grid local-U3 parameters against pi/4-grid, low-denominator dyadic-pi, rational-pi denominator <=512, and source-absorption contracts. All five remain unaccepted: accepted exact decomposition, symbolic decomposition, full-circuit replay certificate count, occurrence removal, proxy-T reduction, and B7 improvement are all 0. This closes the cheap exact-decomposition route but not broader symbolic synthesis, context-aware absorption, or full-circuit replay with honest resource pricing.

B1/B7 update: T-B1-004ao consumes T-B1-004an and tests the five remaining line-1381 parameters against the native optimized gcm_h6 rotation inventory plus a +/-64-line same-support context window. It reviews 2,049 inventory rotation arguments and 44 same-support context rotation arguments; 0/5 parameters have exact or absolute-angle inventory matches, 0/5 have same-support context matches, 0/5 exactly cancel back to the pi/4 grid with one context rotation, and accepted full-circuit replay, occurrence removal, proxy-T reduction, and B7 improvement remain 0. This closes only the cheap single-step context absorption route; multi-rotation/context-symbolic absorption and full-circuit replay remain open.

B1/B7 update: T-B1-004ap consumes T-B1-004ao and tests the five remaining line-1381 parameters against signed sums of two or three nearby same-support context rotations. It reviews the same 44 context rotation arguments, evaluates 3,784 width-2 and 105,952 width-3 signed combinations per parameter, 548,680 total signed combination tests, and finds 0/5 exact absorptions back to the pi/4 grid. The best width-3 grid error is 0.0015819911093339911, while accepted full-circuit replay, occurrence removal, proxy-T reduction, and B7 improvement remain 0. This closes only a bounded two-/three-rotation context route; four-or-more-rotation symbolic absorption, commutation-aware full-circuit replay, and different occurrence-removing scaffolds remain open.

B1/B7 update: T-B1-004aq consumes T-B1-004ap and tests the five remaining line-1381 parameters against signed sums of exactly four nearby same-support context rotations. It reviews the same 44 context rotation arguments, evaluates 2,172,016 width-4 signed combinations per parameter, 10,860,080 total signed combination tests, and finds 0/5 exact absorptions back to the pi/4 grid. The best width-4 grid error remains 0.0015819911093339911, while accepted full-circuit replay, occurrence removal, proxy-T reduction, and B7 improvement remain 0. This closes the exactly-four-rotation context route; five-or-more-rotation symbolic absorption now needs a stronger justification than local bounded search, and commutation-aware full-circuit replay or another occurrence-removing scaffold is the higher-value next route.

B1/B7 update: T-B1-004ar consumes the best bounded context hints from T-B1-004ap/aq and checks whether they have a cheap commutation corridor into the line-1381 packet. It reviews 10 best context candidates, 32 context references, and 8 unique context lines. The gate accepts 0 corridor candidates: 7 references are inside the target packet, 13 are not standalone RZ-like rotations, 21 have support-touching CNOT or non-diagonal single-qubit blockers, and 0 external standalone-Z references have a clear path into the packet. Accepted full-circuit replay, occurrence removal, proxy-T reduction, and B7 improvement remain 0. This closes only the cheap corridor interpretation of the current bounded context hints; symbolic/full-circuit replay or another occurrence-removing scaffold remains required.

B1/B7 update: T-B1-004as consumes the repaired-packet resource boundary and the line-1381 exact/context/corridor pressure gates as a full-circuit replay obligation matrix. All 3 reduced-CNOT packets still have bounded exact repairs, but 0 symbolic exactness certificates, 0 full-circuit replay events, 0 replacement QASM patches, 0 occurrence-class lifts, and 0 B7 ledger acceptances exist. Two packets are resource-clean at bounded-packet level, while line 1381 remains the unpriced off-grid packet; total blocking obligations are 17, max per-packet blocker count is 7, accepted replay/occurrence/proxy-T reduction remains 0. This makes the next gate explicit: produce symbolic/full-circuit replay artifacts or a different occurrence-removing route, not another unpriced local diagnostic.

B1/B7 update: T-B1-004at consumes the full-circuit replay obligation matrix and emits bounded OpenQASM 3 replacement snippets for all 3 repaired reduced-CNOT packets. All 3 snippets pass bounded exactness, and the candidate CNOT reduction remains 9 if a future full-circuit patch is accepted. The new blocker is composability: the line-1378 and line-1381 source windows overlap on lines 1369-1377, so the snippets are not accepted as an independent full-circuit patch set. Accepted full-circuit patches, replay certificates, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0. The next gate must merge or resynthesize the overlapping patch region and replay it against the source circuit before any B7 credit can be counted.

B1/B7 update: T-B1-004au consumes the bounded replacement patch gate and computes the best non-overlapping bounded patch subset. It selects line 268 plus the larger line-1381 window, drops line 1378 because it is contained in the line-1381 source window, and reduces the current composable bounded candidate CNOT delta from the naive 9 to 6. This is an accounting correction, not a solution: the source circuit is OpenQASM 2.0 while the bounded snippets are OpenQASM 3 snippets, no full-circuit QASM rewrite is emitted, and accepted replay/occurrence/proxy-T/B7 reduction remains 0. The next gate must bridge the dialect/replay gap or synthesize a merged region that recovers the dropped line-1378 delta.

B1/B7 update: T-B1-004av consumes the non-overlap subset and emits a concrete OpenQASM 2.0 candidate source rewrite for `gcm_h6`. The line-268 plus line-1381 bounded snippets are converted from OpenQASM 3 `U` syntax to OpenQASM 2.0 `u3` syntax and inserted into the source circuit, producing `results/B1_B7_cone01_qasm2_candidate_rewrite_gate/gcm_h6_line268_line1381_candidate.qasm`. Source CNOT count is 795, candidate CNOT count is 789, and the structural delta is 6. This is the first source-circuit artifact that a full-circuit replay gate can consume, but accepted full-circuit patch/replay/occurrence/proxy-T/B7 reduction remains 0 because whole-circuit equivalence, local-U3 pricing, and the dropped line-1378 merged-region question are still open.

B1/B7 update: T-B1-004aw consumes the T-B1-004av OpenQASM 2.0 candidate and runs a full-statevector replay probe on the benchmark default input after final measurements are removed. The 19-qubit source and candidate circuits produce statevectors of dimension 524,288 with fidelity 0.9999999999999551, infidelity 4.4853010194856324e-14, max global-phase-aligned amplitude delta 1.3908205762322243e-13, max probability delta 5.551115123125783e-16, and measured q[4] marginal delta 5.551115123125783e-16. This is the first whole-circuit numerical replay pressure for the QASM2 candidate, but it is still not a symbolic arbitrary-input equivalence proof, not accepted local-U3 pricing, not occurrence removal, not proxy-T reduction, and not B7 ledger improvement. The next gate must upgrade this default-input probe into symbolic or multi-input full-circuit replay evidence, or synthesize the overlapping line-1378/line-1381 region and price the remaining local-U3 burden honestly.

B1/B7 update: T-B1-004ax consumes the T-B1-004aw replay probe and adds deterministic sampled-input pressure. It evolves 6 computational-basis preparations and 2 seeded product states through both the source and QASM2 candidate after final measurements are removed. All 8 input cases pass: min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, max global-phase-aligned amplitude delta is 1.392888964263601e-13, and max probability delta is 1.8214596497756474e-15. This strengthens the replay evidence but remains sampled evidence, not symbolic arbitrary-input equivalence, accepted local-U3 pricing, occurrence removal, proxy-T reduction, or B7 ledger improvement. The next gate must become symbolic/unitary-equivalence evidence or produce a different occurrence-removing route.

B1/B7 update: T-B1-004ay consumes the T-B1-004ax sampled-input replay pressure and checks phase consistency plus superposition replay. It evolves 4 phase-anchor inputs and 4 superposition inputs through both the source and QASM2 candidate after final measurements are removed. All 8 input cases pass: overlap phase spread is 1.3722356584366935e-13 radians, min overlap magnitude is 0.9999999999999772, min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, and max probability delta is 1.074140776324839e-14. This reduces the risk that per-input global-phase alignment hides an input-dependent phase mismatch, but it remains sampled evidence, not symbolic arbitrary-input equivalence, accepted local-U3 pricing, occurrence removal, proxy-T reduction, or B7 ledger improvement. The next gate must become symbolic/unitary-equivalence evidence or produce a different occurrence-removing route.

B1/B7 update: T-B1-004az consumes the T-B1-004ay phase-consistent replay pressure and fixes one global phase anchor from the zero input. It reuses that same phase across 6 computational-basis anchors and 15 coherent pair superpositions after final measurements are removed. All 21 input cases pass: max global-anchor phase delta is 3.142993331217661e-14 radians, min overlap magnitude is 0.9999999999999772, min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, and max probability delta is 1.074140776324839e-14. This is stronger sampled subspace evidence than independently aligned input replay, but it remains sampled evidence, not symbolic arbitrary-input equivalence, accepted local-U3 pricing, occurrence removal, proxy-T reduction, or B7 ledger improvement. The next gate must become symbolic/unitary-equivalence evidence or produce a different occurrence-removing route.

B1/B7 update: T-B1-004ba consumes the T-B1-004az global-phase anchored subspace replay pressure and converts the six basis anchors into a finite linear-span replay certificate. It computes the source/candidate error operator restricted to the six-dimensional input span under the same zero-input global phase anchor. The restricted spectral norm is 2.7889440543898627e-13, max basis L2 error is 2.534056605707275e-13, max basis probability delta is 7.771561172376096e-16, and all 15 coherent pair witnesses remain passed. This is now certificate-grade for that finite 6-dimensional span, but it covers only 6 of 524,288 input dimensions and remains outside symbolic full-space equivalence, accepted local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement.

B1/B7 update: T-B1-004bb consumes the non-overlap patch subset, QASM2 candidate rewrite, and T-B1-004ba replay certificate to create a composable patch certificate. The selected line-268 plus line-1381 windows are non-overlapping, both local-unitary replacement certificates pass, the QASM2 candidate exists, max selected patch residual norm is 6.513210005207597e-13, max entry error is 4.525273102184799e-13, and the structural CNOT count remains 795 -> 789. This accepts one tolerance-bounded full-circuit semantic replay/QASM patch artifact, but B7 resource credit remains 0 because line 1378 is still dropped and line 1381 retains five unpriced off-grid local-U3 parameters.

B1/B7 update: T-B1-004bc consumes T-B1-004bb and quantifies the remaining line-1381 local-U3 pricing blocker. The semantic patch certificate remains accepted, selected lines stay [268, 1381], and line 1378 remains the dropped overlap candidate. Line 1381 retains 5 off-grid local-U3 parameters, priced as 100 proxy-T pressure units under the project ledger; selected CNOT delta remains 6 and the lost line-1378 delta remains 3. This is a sharper boundary, not a win: local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0. Next work must eliminate, absorb, or symbolically decompose the five line-1381 parameters, recover the line-1378 merged-region delta, or find a different occurrence-removing route.

B1/B7 update: T-B1-004bd consumes the non-overlap subset and T-B1-004bc pricing gate to test whether the dropped line-1378 delta can be recovered additively by merging the overlapping line-1378/line-1381 region. It proves an accounting boundary: the line-1378 window [1369, 1377] is contained inside the line-1381 window [1369, 1379] on the same support [4, 8], so the union region has only 5 source CNOTs. Adding the line-1381 3-CNOT delta and the line-1378 3-CNOT delta would require a -1 replacement-CNOT region, which is impossible under nonnegative CNOT accounting. The max extra CNOT delta versus the current line-1381 replacement is 2, so the full lost line-1378 delta is not additively recoverable. This blocks the naive 9-CNOT story: accepted occurrence removal, proxy-T reduction, merged-region rewrite, and B7 ledger improvement remain 0. Next work must synthesize a new union-region replacement with replay and honest local-U3 pricing, or abandon additive recovery for a different occurrence-removing route.

B1/B7 update: T-B1-004be consumes the semantic packet, packet-synthesis search, and T-B1-004bd overlap bound to test the honest union-region route directly. It searches 0-CNOT and 1-CNOT local-U3 scaffolds for the line-1378/1381 union target [1369, 1379], including both 1-CNOT orientations. Exact low-CNOT passes are 0/3; the best low-CNOT residual is 0.2548908758679516 with max entry error 0.12724247975106365. The existing 2-CNOT line-1381 replacement remains the current exact candidate in this branch, so extra delta beyond the current replacement is 0. This is not a global CNOT lower-bound theorem, but it closes the scoped 0/1-CNOT union-region shortcut: accepted union rewrite, replay certificate, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bf consumes the same union-region target and runs an all-orientation 2-CNOT sequence census. It checks all four length-2 CNOT direction sequences over the line-1378/1381 union window [1369, 1379]; all 4/4 sequences pass the numerical exact threshold. The best exact sequence is 01-10 with residual 5.812946138498332e-13, max entry error 3.4095575404049453e-13, 13 off-pi/4 local-U3 parameters, and 18 total local-U3 parameters. This confirms the 2-CNOT union-region candidate is robust across orientation sequences, but it still has no accepted full-circuit replay certificate, QASM patch, local-U3 resource pricing, occurrence removal, proxy-T reduction, or B7 ledger improvement.

B1/B7 update: T-B1-004bg consumes T-B1-004bf plus the line-1381 local-U3 pricing boundary and checks pricing dominance. The current line-1381 branch has 5 off-pi/4 local-U3 parameters, priced as 100 proxy-T pressure units. The best-priced exact 2-CNOT census candidate has 13 off-pi/4 parameters, priced as 260 proxy-T pressure units, so it is worse by 8 off-grid parameters and 160 proxy-T pressure units. The 2-CNOT census remains robustness evidence, but it is not adopted as a better B7 resource route; selected replacement change, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bh consumes the five-parameter line-1381 exact repair and T-B1-004bg pricing boundary, then performs a leave-one-out parameter pressure test. Each of the five current off-pi/4 local-U3 parameters is snapped back to the pi/4 grid while the other four are re-optimized on the same two-CNOT scaffold. Exact passes are 0/5; the best residual is 0.09892087709180968, about 9.89e6 times the 1e-8 exact tolerance. This blocks a cheap single-parameter removal claim, but it is not a global five-parameter minimality theorem and still leaves accepted occurrence removal, proxy-T reduction, local-U3 acceptance, and B7 ledger improvement at 0.

B1/B7 update: T-B1-004bi consumes the same five-parameter line-1381 exact repair and T-B1-004bh leave-one-out boundary, then performs a leave-two-out parameter pressure test. Every pair among the five current off-pi/4 local-U3 parameters is snapped back to the pi/4 grid while the remaining three are re-optimized on the same two-CNOT scaffold. Exact passes are 0/10; the best residual is 0.13583443746892182 for fixed pair [9, 16], about 1.36e7 times the 1e-8 exact tolerance, and the worst residual is 0.41204448255804876 for fixed pair [16, 17]. This blocks a cheap two-parameter removal claim, but it is not a global five-parameter minimality theorem and still leaves accepted occurrence removal, proxy-T reduction, local-U3 acceptance, and B7 ledger improvement at 0.

B1/B7 update: T-B1-004bj consumes the same five-parameter line-1381 exact repair and T-B1-004bi leave-two-out boundary, then performs a leave-three-out parameter pressure test. Every triple among the five current off-pi/4 local-U3 parameters is snapped back to the pi/4 grid while the remaining two are re-optimized on the same two-CNOT scaffold. Exact passes are 0/10; the best residual is 0.29673862906454757 for fixed triple [4, 9, 16], about 2.97e7 times the 1e-8 exact tolerance, and the worst residual is 0.7449029676343185 for fixed triple [4, 16, 17]. This blocks a cheap three-parameter removal claim, but it is not a global five-parameter minimality theorem and still leaves accepted occurrence removal, proxy-T reduction, local-U3 acceptance, and B7 ledger improvement at 0.

B1/B7 update: T-B1-004bk consumes the same five-parameter line-1381 exact repair and T-B1-004bj leave-three-out boundary, then performs a leave-four-out parameter pressure test. Every quadruple among the five current off-pi/4 local-U3 parameters is snapped back to the pi/4 grid while the remaining one is re-optimized on the same two-CNOT scaffold. Exact passes are 0/5; the best residual is 0.45761708677312707 for fixed quadruple [3, 4, 9, 16], about 4.58e7 times the 1e-8 exact tolerance, and the worst residual is 0.8369082341779268 for fixed quadruple [4, 9, 16, 17]. This blocks a cheap four-parameter removal claim, but it is not a global five-parameter minimality theorem and still leaves accepted occurrence removal, proxy-T reduction, local-U3 acceptance, and B7 ledger improvement at 0.

B1/B7 update: T-B1-004bl consumes the same five-parameter line-1381 exact repair and T-B1-004bk leave-four-out boundary, then performs the all-grid endpoint pressure test. All five current off-pi/4 local-U3 parameters are snapped back to the pi/4 grid with no remaining free parameter to re-optimize on the same two-CNOT scaffold. Exact pass / fail is 0/1; the all-grid residual is 0.8415210419190079, about 8.42e7 times the 1e-8 exact tolerance. This blocks a cheap all-parameter grid-snap claim, but it is not a global five-parameter minimality theorem and still leaves accepted occurrence removal, proxy-T reduction, local-U3 acceptance, and B7 ledger improvement at 0.

B1/B7 update: T-B1-004bm closes the cheap grid-pricing escape hatch for the T-B1-004bf union-region 2-CNOT census candidates. It snaps every local-U3 parameter in each of the four exact union candidates back to the pi/4 grid and replays the union target. Exact pass / fail is 0/4; the best grid-snap residual is 0.36435162331693166 on sequence `10-10`, and the worst residual is 1.021457442072864 on sequence `10-01`. The union census remains useful evidence about the CNOT scaffold boundary, but it is not a free grid-priced B7 resource route; accepted occurrence removal, proxy-T reduction, local-U3 pricing acceptance, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bn tests the next-cheapest union-census pricing route after T-B1-004bm. For each of the four exact 2-CNOT union-region candidates, it snaps all local-U3 parameters to the pi/4 grid, frees exactly one parameter, and re-optimizes that single degree of freedom. Exact pass / fail is 0/72; the best residual is 0.25709607640616583 on sequence `10-10` at parameter index 7, while the worst best-sequence residual is 0.6857140007440164 on sequence `10-01`. This blocks a 20-proxy-T one-free-parameter adoption of the union route; accepted occurrence removal, proxy-T reduction, local-U3 pricing acceptance, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bo tests the next union-census pricing rung after T-B1-004bn. For the same four exact 2-CNOT union-region candidates, it snaps all local-U3 parameters to the pi/4 grid, frees every possible pair of parameters, and re-optimizes each pair. Exact pass / fail is 0/612; the best residual is 0.1831095797026285 on sequence `10-10` at parameter pair `[5, 7]`, while the worst best-sequence residual is 0.46644639853601 on sequence `10-01`. This blocks a 40-proxy-T two-free-parameter adoption of the union route. Future work must change scaffold, prove symbolic/context absorption, accept honest larger local-U3 pricing with full-circuit replay, or find a different occurrence-removing branch; B7 ledger improvement remains 0.

B1/B7 update: T-B1-004bp adds a targeted three-free expansion gate for the same union-census route. It does not exhaust all three-free triples; instead it expands each sequence's best failed T-B1-004bo two-free pair by one additional free local-U3 parameter. Exact pass / fail is 0/64; the best residual is 0.04582709543239648 on sequence `10-10` at parameter triple `[5, 7, 4]`, while the worst best-sequence residual is 0.3812803680403496 on sequence `10-01`. This blocks the most direct 60-proxy-T extension of the failed two-free route, while leaving scaffold change, symbolic/context absorption, honest full-circuit local-U3 pricing, or a different occurrence-removing branch as the next real options; B7 ledger improvement remains 0.

B1/B7 update: T-B1-004bq changes the union-region scaffold itself. It tests all 8 possible length-3 CNOT direction sequences for the same line-1378/1381 union target `[1369, 1379]`, with arbitrary local-U3 layers. The screen finds local exact candidates in all 8 sequences and a best residual of 5.810128819011275e-13, but honest pricing rejects the route: the best exact priced candidate still has 18 off-grid local-U3 parameters / 360 proxy-T pressure. That is worse than the current line-1381 5 off-grid / 100 proxy-T boundary, worse than the 2-CNOT census 13 off-grid / 260 proxy-T best price, and not structurally dominant over the current 2-CNOT replacement. B7 ledger improvement remains 0. Future work must either collapse the local-U3 burden through symbolic/context absorption, find a different scaffold that beats 5 / 100, or switch to another occurrence-removing route.

B1/B7 update: T-B1-004br consumes the best exact priced 3-CNOT candidate from T-B1-004bq, sequence `10-10-01`, and tests whether its 18 off-grid local-U3 parameters can be absorbed by existing native optimized gcm_h6 rotations or by one same-support context rotation around the union window. The gate checks 2,049 rotation arguments overall and 44 same-support context rotation arguments. It finds 0/18 exact inventory matches, 0/18 absolute-angle inventory matches, 0/18 same-support context matches, and 0/18 exact one-step cancellations back to the pi/4 grid. The best one-step grid-cancellation error is 0.000655799901145393, not exact. This closes the cheapest context-absorption escape hatch for the direct 3-CNOT route; B7 ledger improvement remains 0.

B1/B7 update: T-B1-004bs extends that 3-CNOT context test to bounded multi-rotation absorption. It keeps the same best exact priced sequence `10-10-01`, the same 18 off-grid local-U3 parameters, and the same 44 same-support context rotation arguments, then evaluates signed sums of two and three context rotations. The gate checks 3,784 width-2 combinations and 105,952 width-3 combinations per parameter, for 1,975,248 signed-combination tests overall. Exact width-2 absorption is 0/18, exact width-3 absorption is 0/18, and the best width-2/width-3 grid error remains 0.000655799901145393. This closes the bounded two-/three-rotation context route for the direct 3-CNOT branch; accepted occurrence removal, proxy-T reduction, local-U3 pricing, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bt takes the next bounded step for that same 3-CNOT branch and tests signed sums of exactly four same-support context rotations. It keeps sequence `10-10-01`, 18 off-grid local-U3 parameters, and 44 same-support context rotation arguments, then evaluates 2,172,016 width-4 combinations per parameter, for 39,096,288 signed-combination tests overall. Exact width-4 absorption is 0/18; the best width-4 grid error remains 0.000655799901145393 and the worst best-parameter error is 0.027779719778975753. This closes the bounded exactly-four-rotation context route for the direct 3-CNOT branch; accepted occurrence removal, proxy-T reduction, local-U3 pricing, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bu upgrades the candidate portability boundary instead of extending cheap context-width enumeration. It consumes the legacy-dialect line-268 plus line-1381 candidate and emits `results/B1_B7_cone01_openqasm3_candidate_export_gate/gcm_h6_line268_line1381_candidate_openqasm3.qasm` with a valid `OPENQASM 3.0` header, `stdgates.inc`, `qubit[19] q`, `bit[1] c`, 487 `u3` to `U` conversions, and one modern measurement assignment. Operation counts are preserved exactly: 789 `cx`, 601 `rz`, 487 `U`, and 1 measurement. This accepts one OpenQASM 3 export artifact, but it is not a replay proof, not local-U3 pricing, not occurrence removal, and not B7 ledger improvement. The next gate should parse or replay the OpenQASM 3 artifact through a modern toolchain before any resource credit is considered.

B1/B7 update: T-B1-004bv consumes the T-B1-004bu artifact and makes the parser dependency boundary explicit. The project-local strict parser passes with 0 errors, 19 qubits, 1 bit, 1,884 statements, 1,878 operation rows, and preserved counts of 789 `cx`, 601 `rz`, 487 `U`, and 1 measurement. Qiskit core is installed, but the optional `qiskit_qasm3_import` loader dependency is absent, so Qiskit's OpenQASM 3 loader fails with `MissingOptionalLibraryError`. This accepts one local parser-readiness artifact and zero Qiskit-loader artifacts; replay proof, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bw consumes the OpenQASM 3 parser-readiness artifact and checks the dialect bridge structurally. The legacy OpenQASM 2 candidate and the OpenQASM 3 artifact normalize to identical canonical instruction streams: 1,878 instructions in each stream, 0 mismatches, 0 length delta, identical SHA256 stream hash `7cd50bea1f5a3c191c5735c0891d3f70f8c07a9cfca9d6e93724e6d49cb36343`, and operation counts of 789 `cx`, 601 `rz`, 487 `U`, and 1 measurement. This accepts one structural roundtrip artifact. It is still not a Qiskit loader parse, semantic replay proof, local-U3 pricing certificate, occurrence removal, proxy-T reduction, or B7 ledger improvement.

B1/B7 update: T-B1-004bx consumes the structural roundtrip and performs project-local semantic replay for the OpenQASM 3 artifact. The strict subset parser constructs a `QuantumCircuit` directly from the OpenQASM 3 file, removes final measurements, and compares the 19-qubit default-input statevector with the optimized source circuit. The replay passes with state fidelity 0.9999999999999551, infidelity 4.4853010194856324e-14, max aligned amplitude delta 1.3908205762322243e-13, max probability delta 5.551115123125783e-16, and measured q[4] marginal delta 5.551115123125783e-16. This accepts one project-local OpenQASM 3 replay artifact, but Qiskit-loader parsing, symbolic/arbitrary-input equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004by consumes the project-local OpenQASM 3 semantic replay artifact and broadens it from the default input to 8 deterministic sampled inputs. The suite includes 6 computational-basis preparations and 2 seeded product states, all evolved through the parsed OpenQASM 3 candidate and the optimized source after final measurement removal. All 8 cases pass: min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, max aligned amplitude delta is 1.392888964263601e-13, and max probability delta is 1.8214596497756474e-15. This accepts one project-local OpenQASM 3 multi-input replay artifact, but Qiskit-loader replay, symbolic/arbitrary-input equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004bz consumes the project-local OpenQASM 3 multi-input replay artifact and adds phase-consistency pressure. The gate checks 4 phase-anchor inputs plus 4 superposition inputs after final measurement removal, measuring overlap phases instead of relying only on per-input global-phase alignment. All 8 cases pass: overlap phase spread is 1.3722356584366935e-13 radians, min overlap magnitude is 0.9999999999999772, min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, max aligned amplitude delta is 1.392888964263601e-13, and max probability delta is 1.074140776324839e-14. This accepts one project-local OpenQASM 3 phase-consistent replay artifact, but Qiskit-loader replay, symbolic/arbitrary-input equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ca consumes the project-local OpenQASM 3 phase-consistent replay artifact and fixes one global phase anchor from the zero-input replay. The same anchor is reused across 6 basis-subspace anchors and 15 coherent pair superpositions. All 21 cases pass: max global-anchor phase delta is 3.142993331217661e-14 radians, min overlap magnitude is 0.9999999999999772, min state fidelity is 0.9999999999999547, max infidelity is 4.529709940470639e-14, max aligned amplitude delta is 1.3928889642636009e-13, and max probability delta is 1.074140776324839e-14. This accepts one project-local OpenQASM 3 global-phase subspace replay artifact, but Qiskit-loader replay, symbolic/arbitrary-input equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cb consumes the project-local OpenQASM 3 global-phase subspace replay artifact and turns its six basis anchors into a finite linear-span certificate. The restricted error operator over the six-dimensional span passes with spectral norm 2.7889440543898627e-13, Frobenius norm 6.134324404657074e-13, max basis L2 error 2.534056605707275e-13, max basis amplitude delta 1.3928889642636009e-13, max basis probability delta 7.771561172376096e-16, max source/candidate Gram delta 1.9984014443252818e-15, and max cross-Gram delta 4.403624367368429e-14. This accepts one project-local OpenQASM 3 finite-span certificate, but it covers only 6/524,288 input dimensions; Qiskit-loader replay, full-space symbolic/local-unitary equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cc lifts the selected line-268 plus line-1381 composable patch certificate onto the OpenQASM 3 artifact by requiring the QASM2 patch certificate, QASM2/OpenQASM 3 structural roundtrip, and OpenQASM 3 finite-span certificate to agree. It preserves the 1,878-instruction normalized stream with zero mismatches and hash `7cd50bea1f5a3c191c5735c0891d3f70f8c07a9cfca9d6e93724e6d49cb36343`; the selected patch residual remains 6.513210005207597e-13, the selected patch entry error remains 4.525273102184799e-13, and the OpenQASM 3 finite-span spectral error remains 2.7889440543898627e-13. This accepts one project-local OpenQASM 3 composable patch-lift artifact, but Qiskit-loader replay, full-space symbolic/local-unitary equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cd seals the OpenQASM 3 patch-lift evidence chain at file level. It computes SHA-256 hashes for the QASM2 candidate, OpenQASM 3 candidate, QASM2 composable patch certificate, OpenQASM 3 structural roundtrip certificate, OpenQASM 3 finite-span certificate, and OpenQASM 3 patch-lift certificate, then records the combined provenance seal hash `159c9b1d99a607d463fe712a190b35460603712561a4ea8eb4033bf4de495902`. Both QASM files have 1,884 raw lines and normalize to the same 1,878-instruction stream. This accepts one project-local OpenQASM 3 provenance-seal artifact, but Qiskit-loader replay, full-space symbolic/local-unitary equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ce adds a source-map gate to that sealed OpenQASM 3 patch-lift chain. It builds a one-to-one source map across 1,878 normalized instructions, confirms raw-line drift count 0, and records source-map hash `92a499ea6d549426095fbb0fc878f7033027991621a6d5ea1c03cd25d82e9e1e`. The selected patch lines are pinned as line 268 -> instruction 263 and line 1381 -> instruction 1375; dropped overlap line 1378 maps to instruction 1372. This accepts one project-local OpenQASM 3 source-map artifact, but Qiskit-loader replay, full-space symbolic/local-unitary equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cf turns that full source map into a compact OpenQASM 3 patch witness packet. It records three review rows for candidate lines 268, 1378, and 1381 with instruction indices 263, 1372, and 1375. Lines 268 and 1381 are selected non-overlap witnesses; line 1378 is preserved as the dropped-overlap witness blocked by the line-1381 window. The packet hash is `e0d2e63f3f2c16be685baef3360ff68d5765db549c5e17e655a6e74c6fb82dc8`, selected CNOT delta remains 6, lost overlap delta remains 3, max witness residual is 9.049428032408627e-13, and max witness entry error is 6.398911863522162e-13. This accepts one project-local review packet only; Qiskit-loader replay, full-space symbolic/local-unitary equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cg removes the Qiskit-loader blocker for the OpenQASM 3 branch. It consumes the parser-readiness, local semantic replay, and patch witness packet gates, installs the missing `qiskit-qasm3-import` dependency path, and loads the exported candidate through Qiskit's `qasm3` loader. The loader sees 19 qubits, 1 classical bit, depth 1483, and operation counts `cx=789`, `rz=601`, `u=487`, `measure=1`; default-input replay against the optimized source passes with fidelity 0.9999999999999551, max aligned amplitude delta 1.3908205762322243e-13, max probability delta 5.551115123125783e-16, and measured q[4] marginal delta 5.551115123125783e-16. This accepts one Qiskit-loader parse artifact and one Qiskit-loader replay artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ch turns that Qiskit-loader replay into an 8-case sampled-input gate. It consumes the default-input Qiskit-loader gate plus the project-local OpenQASM 3 multi-input replay gate, then sends the same 6 computational-basis inputs and 2 seeded product states through the Qiskit-loaded candidate and the optimized source after final measurement removal. All 8 cases pass: min fidelity 0.9999999999999547, max infidelity 4.529709940470639e-14, max aligned amplitude delta 1.392888964263601e-13, max probability delta 1.8214596497756474e-15, and failed cases 0. This accepts one Qiskit-loader multi-input replay artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ci turns that Qiskit-loader branch into a phase-consistent sampled replay gate. It consumes the Qiskit-loader multi-input replay gate plus the project-local OpenQASM 3 phase-consistent replay gate, then sends 4 phase-anchor inputs and 4 superposition inputs through the Qiskit-loaded candidate and optimized source. All 8 cases pass: overlap phase spread 1.3722356584366935e-13 radians, min overlap magnitude 0.9999999999999772, min fidelity 0.9999999999999547, max infidelity 4.529709940470639e-14, max aligned amplitude delta 1.392888964263601e-13, max probability delta 1.074140776324839e-14, and failed cases 0. This accepts one Qiskit-loader phase-consistent replay artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cj turns that Qiskit-loader branch into a global-phase anchored subspace replay gate. It consumes the Qiskit-loader phase-consistent replay gate plus the project-local OpenQASM 3 global-phase subspace replay gate, fixes the zero-input global phase anchor, and reuses it across 6 basis anchors plus 15 coherent pair superpositions. All 21 cases pass: max global-anchor phase delta 3.142993331217661e-14 radians, min overlap magnitude 0.9999999999999772, min fidelity 0.9999999999999547, max infidelity 4.529709940470639e-14, max aligned amplitude delta 1.3928889642636009e-13, max probability delta 1.074140776324839e-14, and failed cases 0. This accepts one Qiskit-loader global-phase subspace replay artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004ck turns that Qiskit-loader branch into a finite linear-span replay certificate. It consumes the Qiskit-loader global-phase subspace replay gate plus the project-local OpenQASM 3 linear-span certificate, fixes the same zero-input global phase anchor, and computes the source/candidate replay error operator over the 6-dimensional basis-anchor span. The certificate passes: linear-span spectral norm 2.7889440543898627e-13, Frobenius error 6.134324404657074e-13, max basis L2 error 2.534056605707275e-13, max basis amplitude delta 1.3928889642636009e-13, max basis probability delta 7.771561172376096e-16, max source/candidate Gram delta 1.9984014443252818e-15, max cross-Gram delta 4.403624367368429e-14, and validation errors 0. This accepts one Qiskit-loader finite-span certificate for 6/524,288 input dimensions, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cl consumes the project-local OpenQASM 3 composable patch-lift gate, the Qiskit-loader global-phase subspace replay gate, and the Qiskit-loader finite-span certificate to support the composable patch lift through the modern loader path. The support gate preserves selected lines [268, 1381], dropped overlap line [1378], 0 stream mismatches over 1,878 normalized instructions, Qiskit-loader finite-span spectral norm 2.7889440543898627e-13, max basis L2 error 2.534056605707275e-13, max probability delta 7.771561172376096e-16, max cross-Gram delta 4.403624367368429e-14, and validation errors 0. This accepts one Qiskit-loader composable patch-lift support artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cm seals the Qiskit-loader evidence chain for reproducibility. It hashes the exported OpenQASM 3 candidate plus six loader artifacts: default replay, multi-input replay, phase-consistent replay, global-phase subspace replay, finite-span certificate, and composable patch-lift support. The evidence seal is `d06c1fdae3ad7cad1971cdcdcea1f890d3931924a7e70affc25fdf89737e09a8`; it preserves Qiskit 2.4.1, qiskit-qasm3-import 0.6.0, openqasm3 1.0.1, depth 1483, operation counts cx=789/rz=601/u=487/measure=1, 8 multi-input cases, 8 phase-consistent cases, 21 global-phase cases, 0 failed replay cases, selected lines [268, 1381], dropped overlap line [1378], and B7 ledger improvement 0. This is drift-detection evidence, not arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, or proxy-T reduction.

B1/B7 update: T-B1-004cn reproduces the Qiskit-loader evidence seal. It independently recomputes all 7 source artifact hashes, reruns the T-B1-004cm seal generator, and verifies that the expected, independent, and reproduced seals all equal `d06c1fdae3ad7cad1971cdcdcea1f890d3931924a7e70affc25fdf89737e09a8`. The reproduction passes with 7/7 source-hash matches, 0 mismatch paths, byte-stable JSON hash `f7a5f57ced33e3d8c3f8be12fbcd0dba26a5b42206dac8bb0e1ed1723a735ad2`, byte-stable Markdown hash `7a648d78758b0f6499d7a743993714fef3d47932b9b02ec5de317228c7828dc7`, 0 failed replay cases, selected lines [268, 1381], dropped overlap line [1378], and B7 ledger improvement 0. This accepts one evidence-seal reproduction artifact, but it remains reproducibility evidence, not arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, or proxy-T reduction.

B1/B7 update: T-B1-004co adds Qiskit-loader seeded product-state replay pressure. It consumes the phase-consistent replay gate and the reproduced evidence seal, loads the same OpenQASM 3.0 candidate through Qiskit's qasm3 loader, removes final measurements, and replays 16 deterministic product-state inputs generated from seeds `[17, 29, 41, 53, 67, 79, 83, 97, 101, 113, 127, 131, 149, 163, 181, 191]` using an rx/ry/rz preparation sequence. All 16 cases pass: min fidelity `0.9999999999999389`, max infidelity `6.106226635438361e-14`, max aligned amplitude delta `1.3496991625769186e-14`, max L2 aligned amplitude delta `2.8917153762798005e-13`, max probability delta `8.020927672047762e-16`, and failed cases 0. This accepts one seeded product-state replay artifact, but arbitrary-input/symbolic equivalence, local-U3 pricing, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cp adds the resource-boundary decision gate for that seeded replay branch. It consumes the seeded product-state replay result, the line-1381 local-U3 pricing gate, the theta-sharing cost-model gate, and the refreshed B7 ledger gate. The seeded replay evidence is accepted as semantic pressure, but all five resource blockers remain failed: line 1381 still carries 5 off-grid local-U3 parameters and 100 unpriced proxy-T pressure, line 1378's overlap delta is not recovered, accepted occurrence removal is 0 against the 30-occurrence target, the theta cost model remains rejected at 6/8 acceptance checks, and the refreshed B7 ledger still rejects theta sharing with 600 proxy-T of missing reduction. This accepts one resource-boundary artifact only; resource saving, occurrence removal, proxy-T reduction, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cq tests the next local context escape hatch for line 1381. It uses a meet-in-the-middle split over exactly five signed same-support context rotations, covering 34,752,256 virtual width-5 combinations per parameter and 173,761,280 virtual tests across the five remaining off-grid local-U3 parameters. The 44 context rotation arguments in the 1305-1443 line window still yield 0/5 exact absorptions back to the pi/4 grid; best width-5 grid error ranges from `0.001581991109333103` to `0.026659551749407484`. This closes the bounded width-5 context route only. It does not rule out six-or-more symbolic absorption, commutation-aware rewriting, full-circuit replay, or a different scaffold; occurrence removal, proxy-T reduction, resource saving, and B7 ledger improvement remain 0.

B1/B7 update: T-B1-004cr/T-B7-010 adds a route-triage decision gate over the current cone_01 shortcut stack. It reviews seeded replay, bounded width-5 context absorption, cheap commutation corridor, shared-theta cache saving, and all-grid line-1381 parameter removal. The result is 5 shortcut routes triaged, 0 accepted for B7 credit, and 5 rejected. The program state now treats the current shortcut stack as exhausted for B7 credit; the next route must be commutation-aware full-circuit replay, honest line-1381 local-U3 pricing under a physical synthesis model, line-1378 recovery without overlap double-counting, or an alternate occurrence-removing scaffold.

B1/B7 update: T-B1-004cs/T-B7-011 implements the honest line-1381 local-U3 pricing branch from that triage as a conservative physical synthesis pricing gate. The selected patch keeps 5 off-grid line-1381 local-U3 parameters. With aggregate synthesis-error budget `1e-8`, per-parameter budget `2e-9`, and single-parameter T-count bound 97, the total physical synthesis T-count bound is 485. The selected 6-CNOT delta supplies only 120 proxy-credit units, so cost-minus-credit is 365 and physical synthesis pricing is rejected for B7 credit. Accepted occurrence removal, proxy-T reduction, resource saving, and B7 ledger improvement remain 0.

B4/B8 update: T-B4-002b/T-B8-003f upgrades the analytic verifier-private predicate pressure gate into a formal commit-challenge-response-verify protocol model over the same 36 B4/B8 challenge rows. The protocol uses 4 private predicate bits, 4 rounds, and 5 spoofer/leakage families; 8/8 analytic gates pass. Hidden-private acceptance is 0.0625, public support-only acceptance is 0.5, one-bit leakage acceptance is 0.125, three-bit leakage acceptance is 0.5, and full private-material leakage acceptance is 1.0. This is still model-level evidence, not hardware execution, cryptographic soundness, protocol soundness, sampling hardness, quantum advantage, or BQP separation.

B4/B8 update: T-B4-002c/T-B8-003g pushes that formal protocol through a conservative transcript-noise bridge. The bridge evaluates 720 transcript/noise/leakage cases from 36 protocol rows, 4 noise profiles, and 5 leakage profiles. Under the backend-like predicate-bit error profile, no-refresh honest acceptance is 0.747047070414 and fails the 0.8 honest threshold, while challenge_refresh reaches 0.805169120213 and refresh_plus_rotation reaches 0.866618491942. No-leak adversary acceptance remains 0.0625, three-private-bit leakage rises to 0.5, and full private-material leakage restores acceptance to 1.0. The technical implication is sharper than the prior analytic gate: refreshed private challenges can survive this conservative noise model, but no-refresh is rejected and full leakage still kills the protocol. The next gate must use real backend properties or hardware randomized-measurement execution and then face learned/generative spoofers; this is still not protocol soundness, hardware evidence, sampling hardness, quantum advantage, or BQP separation.

B4/B8 update: T-B4-002d/T-B8-003h adds a deterministic parametric learned/generative spoofer-pressure diagnostic over the conservative noise bridge. The diagnostic creates 2,880 pressure rows from 720 source transcript/noise/leakage cases and 4 spoofer families. The artifact is structurally valid with 0 validation errors, but it intentionally preserves a negative 6/8 gate split: max no-leak spoofer acceptance reaches 0.1196875, and max backend-like refreshed no-leak spoofer acceptance reaches 0.109140625, so both exceed the 0.10 diagnostic threshold. Three-private-bit leakage reaches 0.6575 and full private-material leakage reaches 1.0. This converts the next research target from "add noise" to "survive actual learned/generative adversaries on real-backend or hardware transcripts"; it is not actual ML training, hardware execution, protocol soundness, sampling hardness, quantum advantage, or BQP separation.

B4/B8 update: T-B4-002e/T-B8-003i replaces the parametric-only warning with a deterministic train/holdout fitted-spoofer diagnostic over the synthetic transcript bridge. It trains on 560 rows, holds out 160 rows by protocol index, and evaluates 4 fitted model families across 640 model-row checks. Private-safe no-leak fitted acceptance stays at 0.0625, including backend-like refreshed no-leak rows; leakage-blind mixture fitting reaches 0.35 on no-leak holdout rows; full private-material leakage remains 1.0. The next technical gate is no longer another synthetic parametric pressure pass, but leakage-separated fitted/generative attacks on real-backend or hardware private-challenge transcripts.

B4/B8 update: T-B4-002f/T-B8-003j consumes the fitted-spoofer holdout result and the B10-T2 GenericBackendV2-style calibrated Aer bridge, then checks real-backend transcript readiness. It passes 5/10 readiness gates and fails R5-R9: real backend properties are absent, hardware execution is absent, real backend transcript rows are 0, leakage-blind no-leak fitted acceptance is 0.35, and full-private-material leakage fitted acceptance is 1.0. This makes the next B4/B8 gate explicit: ingest real backend properties or hardware randomized-measurement transcripts, keep leakage-separated train/holdout splits, and rerun fitted/generative spoofers before any soundness or advantage claim is promoted.

B3/B10 update: T-B3-013/T-B10-015b converts the failed chemistry rescue route into a same-access negative boundary note. It satisfies 9/9 negative-boundary conditions over the source rescue gate and keeps B3 demoted until M5-M9 are replaced: full cross-molecule compiled-state covariance, multi-parameter/converged chemistry state prep, same-access denominator wins, optimizer-loop cost reduction, and B10 access-contract acceptance. The technical plan is now to keep B3 closed unless those blockers are replaced, and otherwise move effort into B5/B10 production denominators.

B5/B10 update: T-B5-006e/T-B10-014c executes W2 as a seeded-pressure replacement audit. It replays non-oracle embedding, variational MPS/ALS, and two-site finite-DMRG-style candidates under the locked 9-row contract. The result is negative: 0 deployable replacements, seeded pressure not replaced, seeded mean error 0.0004416259745141553, best replacement-by-mean `variational_mps_als` at 0.01805548365563228 with 0 rows beating seeded pressure, and max row-level win count 2/9 across all candidates. The remaining positive-route implementation packets are W1 production DMRG/MPS and W3 same-access response oracle. No production DMRG, response oracle, same-access positive route, quantum advantage, BQP separation, or catalog change is claimed.

B5/B10 update: T-B5-006f/T-B10-014d executes W3 as a same-access response-oracle cost ledger. It checks 8 oracle requirements under the locked 9-row contract. O1 row contract, O2 measurement-confidence ledger, and O8 forbidden-claim discipline pass; O3 state preparation, O4 mixing/query cost, O5 readout/noise cost, O6 optimizer-loop cost, and O7 denominator win fail. No response oracle is constructed, 0 rows beat explicit D5 matvec pressure for the seeded target, and the remaining positive-route implementation packet is W1 production DMRG/MPS. No production DMRG, response oracle, same-access positive route, quantum advantage, BQP separation, or catalog change is claimed.

B5/B10 update: T-B5-006h/T-B10-014f executes W1 denominator-engine v0. It checks 8 denominator-engine requirements under the locked 9-row contract: 4 pass and 4 fail. E1 row contract, E2 non-exact-state-seeded candidate execution, E3 sweep-ledger availability, and E8 forbidden-claim discipline pass; E4 production canonical environments/residuals, E5 all-row convergence, E6 seeded-pressure replacement, and E7 same-access production cost ledger fail. The ledger has 216 sweep rows, 0 convergence-passed rows, 0 rows beating seeded pressure, and keeps B10-T1 negative. No production DMRG, deployable denominator, same-access positive route, quantum advantage, BQP separation, or catalog change is claimed.

B5/B10 update: T-B5-006i/T-B10-014g executes W1 canonical residual blocker decomposition. It checks 8 requirements under the locked 9-row contract: 4 pass and 4 fail. C3 stored canonical environments, C4 orthonormal residual ledgers, C5 all-row convergence evidence, and C7 same-access production cost ledger fail. The gate records 0 environment rows, 0 residual rows, 0 convergence-passed rows, 0 seeded-pressure wins, and emits 4 PR packets: W1-E4-env-residuals, W1-E5-convergence, W1-E6-seeded-pressure, and W1-E7-cost-ledger. No production DMRG, deployable denominator, same-access positive route, quantum advantage, BQP separation, or catalog change is claimed.
